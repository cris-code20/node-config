{
  "name": "02typescrip",
  "version": "1.0.0",
  "description": "este es un guia de node con typescript",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start":"npm run build && node dist/app.js",
    "build":"rimraf ./dist && tsc",
    "dev": "nodemon"
  },
  "author": "crisDec",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^20.10.0",
    "nodemon": "^3.0.2",
    "rimraf": "^5.0.5",
    "ts-node": "^10.9.1",
    "typescript": "^5.3.2"
  },
  "dependencies": {
    "winston": "^3.11.0"
  }
}
